- [ ] 🎯 **P0-F — the scale and soak rider.** Write ≥ 5 GB of archives to OPFS,
      leave the device dormant 7+ days, then cold-start and render offline —
      on a real iPhone **and** an older iPad (founding review).

      This is the hard half of Phase 0's risk and the reason the phase is not
      done when a region renders. A happy-path regional render proves the
      pipeline; it proves nothing about eviction under storage pressure,
      about what iOS reclaims from a dormant app, or about a device whose
      GPU and RAM are a generation behind the one it was built on.

      🎯 **Blocked on Mike**: real devices and, by construction, seven
      calendar days of waiting. Worth starting the clock early — the
      dormancy is wall-time that no amount of session capacity shortens.

      review: not warranted — an acceptance test for a bar the WORKPLAN
      already sets.
