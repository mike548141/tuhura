# 2026-09-25 · 0118 UTC · The seam's findings, ruled and applied

Same Fable session as the cold pass, resumed four days later on Mike's
instruction to complete the work. Nothing had moved in between.

## The ruling

Under rule 3 the eleven findings were Mike's. Asked through the structured
device with the account ahead of it, he chose **accept all, apply now**
over applying only the three proven defects or holding. That ruling also
accepts the ADR, which its author had marked accepted on its own authority
(F8).

## What landed

One worktree, one commit, merged `--no-ff`. The seam's read primitive now
clamps only pmtiles' offset-0 header probe (the library's own precedent)
and the handle is driven under the real vendored `PMTiles` on archives
both sides of 16 KiB. The writer checks a chunk before the write and
counts it after, so a quota failure leaves `bytesWritten` equal to the
disk, the retry lands, and `commit()` cannot publish short. The copy
fallback is gone; `move()` absent is a typed stop with the staging file
resumable. Every seam error carries a stable code and round-trips through
JSON for the worker→main boundary. Writes name their version and a
mismatched staging file is refused. Ten typed errors, idempotent delete,
content-stable `getKey()`. The fixture generator's Hilbert weight is
`s*s`; its self-check now proves a bijection over z0–z4 and the JS suite
cross-checks all 341 ids against the vendored library.

Tests 29 → 55, including `tests/storage-opfs.test.js`: the in-memory OPFS
stand-in the cold pass built to prove F2, kept as the gate. The ADR carries
a dated addendum with the decisions and the threat enumeration; ARCHITECTURE
names the seam; the verdict carries `[fixed]` stamps each closed on a test.

## Rule 4, applied to the application

The application is self-authored direction — this session wrote both the
verdict and the fixes — so it is not this session's to review. Queued
`⏳` `040-queued-reviews/030-seam-fixes-application.md`, refs only, bound
spelled self-referentially, with a rule-2 note for the taker about the
verdict's decision stamps sitting inside the delta.

## Housekeeping

Atelier pin `2b29bb7` → `548b706`: the three commits are a scanner file-walk
refactor and board records; template and method unchanged. Worktree
removed, branch deleted, everything pushed.
